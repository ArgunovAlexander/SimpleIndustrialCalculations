<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alexander.argunov.simpleindustrialcalculations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MenuActivity"
            android:label="@string/choose_action"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".CalcOxyFlow"
            android:label="@string/oxy_flow"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".CalcOxyConc"
            android:label="@string/oxy_conc"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".DetailedSolution"
            android:label="@string/air_loss"
            android:windowSoftInputMode="stateAlwaysHidden" />
        <activity
            android:name=".Answer"
            android:label="@string/result"
            android:windowSoftInputMode="stateAlwaysHidden" />
    </application>

</manifest>